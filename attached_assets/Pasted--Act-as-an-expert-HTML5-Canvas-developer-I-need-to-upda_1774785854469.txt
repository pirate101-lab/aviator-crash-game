"Act as an expert HTML5 Canvas developer. I need to update the rendering of the plane in my crash game. Please replace the current plane rendering logic in our animation loop with the following exact mathematical placement.

1. Calculate the Trajectory Angle:
Before drawing the plane, you must calculate the exact tangent angle of the curve so the plane tilts correctly as it flies. Use the current point (x, y) and the previous frame's point (prevX, prevY):
JavaScript

// Calculate the angle of the trajectory in radians
// Note: Canvas Y-axis goes down, so we adjust accordingly if needed based on your grid
const trajectoryAngle = Math.atan2(currentY - prevY, currentX - prevX);

2. The Render Function (Tail Anchoring):
Implement this specific function to draw the plane. It uses a translation matrix to move the canvas origin to the tip of the curve, rotates it to the trajectoryAngle, and draws the sprite offset so the tail (left side of the image) connects to the tip, not the center.
JavaScript

// Assuming you have loaded the plane image as `planeSprite`
// e.g., const planeSprite = new Image(); planeSprite.src = '/plane.png';

function renderPlanePerfectly(ctx, tipX, tipY, angle, planeSprite) {
    ctx.save();
    
    // 1. Move the drawing origin strictly to the tip of the green line
    ctx.translate(tipX, tipY);
    
    // 2. Rotate to match the flight path
    // If your sprite is already drawn pointing up/right in the PNG, you may need to offset this angle (e.g., angle - (Math.PI/4))
    ctx.rotate(angle); 
    
    // 3. Define the size of the plane
    const planeWidth = 64; 
    const planeHeight = 24;
    
    // 4. Draw the image OFFSET to the left by its full width.
    // X = -planeWidth (puts the right side/nose forward, and left side/tail at the 0,0 origin)
    // Y = -planeHeight / 2 (centers it vertically on the line)
    ctx.drawImage(planeSprite, -planeWidth, -planeHeight / 2, planeWidth, planeHeight);
    
    ctx.restore();
}

3. Waiting Phase (Pre-takeoff):
When the game state is WAITING (timer is counting down), do not use the angle calculation. Call the function with a static position and a slight upward tilt so it rests on the bottom left grid:
JavaScript

// Render at bottom left of the graph area, tilted up slightly (e.g., -15 degrees converted to radians)
renderPlanePerfectly(ctx, startX, startY, -0.26, planeSprite); 

Please integrate these functions and ensure the green path line (ctx.lineTo) ends exactly at (tipX, tipY) right before renderPlanePerfectly is called."